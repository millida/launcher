/**
 * Настройки ежедневного сундука, которые НЕ приходят с сервера.
 *
 * Сами награды (сколько рубинов в какой день, в какой день сундук, стрик,
 * есть ли у игрока PLUS) лаунчер берёт только из ответа
 * `GET /v2/rubies/daily` — придумывать их в коде нельзя. Здесь лежит лишь то,
 * без чего экран не может нарисоваться, если старая служба поле не прислала.
 *
 * СОГЛАСОВАТЬ С ВЛАДЕЛЬЦЕМ: каждое значение ниже.
 */
export const DAILY_CONFIG = {
  /**
   * Во сколько раз PLUS щедрее обычной строки, если служба не прислала
   * `plusMultiplier`. Та же двойка стоит в магазине (shop/DailyBonus.tsx),
   * чтобы два экрана не спорили. Согласовать с владельцем.
   */
  plusMultiplierFallback: 2,
  /**
   * Граница суток. Служба считает день по UTC (`dayKeyOf` в
   * trade-api/src/launcher/rubies/rubies.catalog.ts): по Москве новый сундук
   * открывается в 03:00. Если служба пришлёт `nextAt`, берётся он.
   */
  dayBoundary: 'utc' as const,
}

/*
 * Правила, которые трек обязан держать (исследование 23.09.2026,
 * Работа/Проекты/millida/analysis/2026-09-23_launcher-lobby-research/):
 * - бесплатная награда видна заранее, купить или ускорить её нельзя;
 * - вещей в бонусе нет (решение владельца 24.09.2026): клетки дают осколки и
 *   сундуки. Сундук в строке PLUS — не платный ящик: его не купить, он
 *   выдаётся за день входа (ресёрч 2026-09-23_lootbox-legal.md, рек. 1). Шансы
 *   печатаются у «Открыть», повтор приходит осколками, вещь не выводится;
 * - пропуск дня не сжигает положенное, восстановления серии за деньги нет;
 * - таймер только у бесплатного сундука; у PLUS — цена в рублях и видная отмена;
 *   закрытая клетка PLUS открывает карточку с ценой, а не сразу оплату.
 */
